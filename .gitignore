node_modules/
.next/
out/
build/
*.tsbuildinfo
*.log
npm-debug.log*

# Prisma / SQLite
*.db
*.db-journal
prisma/dev.db
prisma/dev.db-journal

# Env — .env holds only safe local dev defaults and is committed so the app
# runs out of the box. Real secrets go in .env.local (ignored).
.env.local
.env*.local

# Editor / OS
.DS_Store
.vscode/
.idea/

# Test
coverage/
